-- The code below makes require"webauthn" work
-- Ref: https://realtimelogic.com/ba/doc/en/Mako.html#mako_createloader
(mako or xedge).createloader(io)
if dir then
    -- This is not a web app so remove from the Virtual File System
   dir:unlink()
end
